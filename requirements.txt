# network_ids dependencies
# Install via: sudo pip3 install -r requirements.txt --break-system-packages
# Or use: ./install.sh (recommended — handles root env correctly)
scapy>=2.5.0
python-nmap>=0.7.1
prettytable>=3.5.0
psutil>=5.9.0
requests>=2.28.0
pandas>=1.5.0
# netifaces is in Ubuntu 26.04 system packages (python3-netifaces)
# numpy is in Ubuntu 26.04 system packages (python3-numpy)
